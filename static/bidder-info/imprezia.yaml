endpoint: "https://api.imprezia.ai/v1/ads/chat"
# Enabled 2026-08-21, step 3 of the safe 3-step rollout (deploy disabled →
# deploy pbs-settings' extra_info → deploy enabled) — see the git history
# on this file and docs/integration/internal-onboarding.md's Imprezia
# section for why a 2-step rollout crashes PBS in either push order.
# pbs-settings' adapters.imprezia.extra_info.apiKey has been live and
# verified healthy on both regions since the prior deploy. Enabling here
# does NOT make Imprezia live in any real auction — no Stored Imp
# references "imprezia" yet (same inert state Gravity is in today); this
# just lets Builder() run successfully instead of skipping.
maintainer:
  email: "prebid@tpcsrv.com"
capabilities:
  site:
    mediaTypes:
      - native
  app:
    mediaTypes:
      - native
